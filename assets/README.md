# Add your GitHub media here

The diary now uses these two files in this `assets` folder:

```text
assets/teacher.jpeg
assets/song.mp3
```

The diary automatically uses the photo on the classroom page and starts the song only after someone touches the ribbon to open the diary. If either file is missing, the presentation still works: it shows a teacher illustration and a small music prompt instead.
